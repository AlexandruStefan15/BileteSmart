import React from "react";
import { StyleSheet, View, Text, SafeAreaView, ScrollView } from "react-native";

const TicketingScreen = () => {
	return (
		<SafeAreaView>
			<ScrollView>
				<Text>TicketingScreen</Text>
			</ScrollView>
		</SafeAreaView>
	);
};

const styles = StyleSheet.create({});

export default TicketingScreen;
